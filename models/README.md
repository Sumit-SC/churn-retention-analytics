# Models

Persisted ML artifacts generated during Stage 4.

## Files
- `logistic_model.joblib` — baseline model
- `rf_model.joblib` — production model
- `preprocessing.joblib` — feature preprocessing pipeline

## Regenerate

```bash
python pipeline/train_model.py
```
